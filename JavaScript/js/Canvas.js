/**
 * Canvas 画布
 */

(function () {
    'use strict';

    window.CanvasUtil = {
        test : testFn
    };

    function testFn(val) {
        return val;
    }
    
})();

/**
 * 运行区
 */
EventUtil.addEvent(window, 'load', function () {

});


